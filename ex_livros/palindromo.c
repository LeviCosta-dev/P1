#include <stdio.h>

int main(void)
{
    int n, i = 0;
    char palavra[256];
    
    while (i < n)
    {
        fgets(palavra, 256, stdin);
        for (int a = 0; a < 256; a++)
        {
            
        }
    }
}